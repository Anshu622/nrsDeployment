spring.application.name=restfulwebservices

server.port=${PORT:8085}

# Database configuration for production (Render will inject these)
spring.datasource.url=${jdbc:mysql://avnadmin:AVNS_Gbj3z1_XDAXd_kpc7so@mysql-1006a971-anshuanand622-68bb.d.aivencloud.com:13373/defaultdb?ssl-mode=REQUIRED}
spring.datasource.username=${avnadmin}
spring.datasource.password=${AVNS_Gbj3z1_XDAXd_kpc7so}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Kafka configuration (if needed for production)
# spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS}
# spring.kafka.consumer.group-id=nrs-orders
# spring.kafka.consumer.auto-offset-reset=earliest
# spring.kafka.consumer.enable-auto-commit=false
# spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
# spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer

# Management endpoints for health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
